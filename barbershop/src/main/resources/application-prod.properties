server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-init-sql=SET search_path TO public

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=86400000

app.frontend.url=${APP_FRONTEND_URL}
app.api.url=${APP_API_URL}

app.business.name=ElPipeBarber
app.business.address=La Galaxia 2292, Maipu
app.business.phone=+56 9 9809 8449

# Admin 1: Felipe (dueño)
app.seed.admin.email=${APP_ADMIN_EMAIL}
app.seed.admin.password=${APP_ADMIN_PASSWORD}
app.seed.admin.name=${APP_ADMIN_NAME:Felipe Eliecer}

# Admin 2: Daniel (soporte técnico) ? variable de entorno en Render: APP_ADMIN2_EMAIL
app.seed.admin2.email=${APP_ADMIN2_EMAIL:danielorlandodiaz14@gmail.com}
app.seed.admin2.password=${APP_ADMIN2_PASSWORD:Admin123!}
app.seed.admin2.name=${APP_ADMIN2_NAME:Soporte Tecnico}

# Barbero vinculado a agenda
app.seed.barber.email=${APP_BARBER_EMAIL}
app.seed.barber.name=${APP_BARBER_NAME:Felipe Eliecer}

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

logging.level.org.hibernate.SQL=WARN
logging.level.com.barbershop=INFO

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

spring.task.scheduling.enabled=true
